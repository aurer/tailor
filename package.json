{
  "licence": "MIT",
  "dependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-preset-preact": "^1.1.0",
    "parcel-bundler": "^1.7.1",
    "preact": "^8.2.7",
    "preact-router": "^2.6.0"
  },
  "scripts": {
    "start": "rm -rf docs/* && parcel src/pug/index.pug -d docs",
    "build": "rm -rf docs/* && parcel build src/pug/index.pug -d docs"
  },
  "devDependencies": {
    "node-sass": "^4.9.0",
    "pug": "^2.0.3",
    "typescript": "^2.8.1"
  }
}
